- name: Setting Up kubernetes masters
  hosts: kube_masters
  become: yes
  become_method: sudo

  tasks:
    - name: Install etcd and keepalived
      include_role:
        name: "{{ item }}"
      with_items:
      - etcd
