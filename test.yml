- name: Cluster info
  hosts: master1
  gather_facts: true

  vars:
    HOST_COUNT: "{{ groups['ingressnodes'] | length }}"


  tasks:
    - name: Get mandatory deployment
      include_role:
        name: ingress
