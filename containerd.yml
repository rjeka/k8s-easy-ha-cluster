- name: Setting Up kubernetes masters
  hosts: all
  become: yes
  become_method: sudo

  tasks:
    - name: Install etcd and keepalived
      include_role:
        name: "{{ item }}"
      with_items:
      - containerd
      #- docker
      #- kubelet
